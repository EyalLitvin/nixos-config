{ config, lib, ... }:

{
  # RTL88x2bu USB WiFi adapter — loaded in system/boot/kernel
  # This module is a placeholder for future per-adapter configuration.
  # The kernel module itself lives in system/boot/kernel/default.nix.
}
